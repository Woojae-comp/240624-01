package com.artseintist.board.util;

import org.springframework.jdbc.core.JdbcTemplate;

public class Constant {
	public static JdbcTemplate template; //전역변수(전역객체) 선언
	// template은 누가 초기화를 시키는지?
	// 선언만 했기 때문에 아무것도 없는 것임
	// 컨트롤러를에 만드는 이유는 컨트롤러가 가장 먼저 동작하기 때문에
	
	
}
